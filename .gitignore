## Ignore Visual Studio temporary files, build results, and
## files generated by popular Visual Studio add-ons.

# User-specific files (impostazioni locali per l'utente)
*.suo
*.user
*.sln.docstates

# Build results (directory e file generati durante la build)
[Dd]ebug/      # Cartella Debug, usata per build di debug
[Rr]elease/    # Cartella Release, usata per build di rilascio
x64/           # Cartella per build a 64-bit
[Bb]in/        # Cartella bin, che contiene gli eseguibili e altri file di output
[Oo]bj/        # Cartella obj, usata per file di output intermedi

# MSTest test Results (risultati dei test automatizzati)
[Tt]est[Rr]esult*/ # Cartella TestResults, usata per i risultati dei test
[Bb]uild[Ll]og.*   # File di log di build

# Temporary files (file temporanei creati da vari processi)
*.tmp
*.tmp_proj
*.log
*.svclog
*.scc

# Visual Studio profiler (file generati dal profiler di Visual Studio)
*.psess
*.vsp
*.vspx

# Guidance Automation Toolkit (file generati dall'add-on di automazione)
*.gpState

# ReSharper is a .NET coding add-in (file generati da ReSharper)
_ReSharper*/  # Cartella ReSharper, che contiene i dati temporanei di questo strumento
*.[Rr]e[Ss]harper
*.DotSettings.user

# Click-Once directory (file generati dal tool ClickOnce)
publish/

# Publish Web Output (file di output della pubblicazione web)
*.Publish.xml
*.pubxml
*.azurePubxml

# NuGet Packages Directory (cartella che contiene i pacchetti NuGet)
packages/

# NuGet repositories.config (file di configurazione dei pacchetti NuGet)
!packages/repositories.config

# Windows Azure Build Output (output della build per Windows Azure)
csx/
*.build.csdef

# Windows Store app package directory (output per app Windows Store)
AppPackages/

# Backup & report files from project upgrade (file di backup e report di aggiornamento del progetto)
_UpgradeReport_Files/
Backup*/
UpgradeLog*.XML
UpgradeLog*.htm

# SQL Server database files (file del database SQL Server)
App_Data/*.mdf
App_Data/*.ldf

# StyleCop code analysis (file generati dall'add-on StyleCop)
[Ss]tyle[Cc]op.*
![Ss]tyle[Cc]op.targets

# =========================
# Windows detritus (file di sistema di Windows)
# =========================

Thumbs.db      # File cache delle anteprime immagini in Windows
ehthumbs.db
Desktop.ini    # File di configurazione delle cartelle di Windows

$RECYCLE.BIN/  # Cestino su condivisioni di rete

# Mac desktop service store files (file di servizio del desktop su Mac)
.DS_Store

# =========================
# Additional files for specific tools or build systems
# =========================

# .NET-specific files (file specifici per .NET)
.dotnet/       # Cartella usata per file di configurazione e toolchain specifici di .NET

# Visual Studio specific files (file generati da Visual Studio)
.vs/           # Cartella contenente le impostazioni di Visual Studio specifiche per l'utente

# Visual Studio Code settings (impostazioni di Visual Studio Code)
.vscode/       # Cartella contenente le impostazioni di Visual Studio Code

# Windows Image file caches (cache dei file immagine in Windows)
Thumbs.db
ehthumbs.db

# Miscellaneous (altri file non necessari nel repository)
*.publishsettings  # File di configurazione delle pubblicazioni web
